<Window x:Class="styles.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:styles"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <TextBlock Text="Выберите тему" Grid.ColumnSpan="2" VerticalAlignment="Center" HorizontalAlignment="Center" FontSize="20" FontFamily="Bahnschrift SemiBold"/>
        <Button Content="BLM(black's lifes matter" Grid.Row="1" MinHeight="40" VerticalAlignment="Center" HorizontalAlignment="Center" FontFamily="Bahnschrift SemiBold" />
        <Button Content="WPB(White peoples better)" Grid.Row="1" Grid.Column="1" MinHeight="40"  VerticalAlignment="Center" HorizontalAlignment="Center" FontFamily="Bahnschrift SemiBold"/>
        <Ellipse Margin="0,0,100,0" >
            <Ellipse.Fill>
                <ImageBrush ImageSource="/кот.jpg"/>
            </Ellipse.Fill>
        </Ellipse>

    </Grid>
</Window>
